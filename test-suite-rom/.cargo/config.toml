[build]
target = "mips-nintendo64-none.json"

[unstable]
build-std = ["core", "alloc"]

[target."mips-nintendo64-none"]
rustflags = ["-Clto", "-Cembed-bitcode"]

# Cargo appends the argument list with the path to the compiled ELF file which nust64 requires for building the ROM
#runner = [
#    "nust64",
#    "--elf"
#]
