[package]
name = "n64_specs"
version = "0.1.0"
edition = "2024"

[dependencies]
arbitrary-int = "2.1"
bitbybit = "2.0"
bytemuck = { version = "1.25", features = ["derive"] }
strum = { version = "0.27.2", default-features = false, features = ["derive"] }
