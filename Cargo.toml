[workspace]
members = ["core", "app", "runner"]
default-members = ["core", "app"]
resolver = "3"

#[profile.release]
#lto = "fat"
#codegen-units = 1
#panic = "abort"

[profile.profiling]
inherits = "release"
debug = true
strip = false
