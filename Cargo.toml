[workspace]
members = ["specs", "core", "app", "runner", "test-suite"]
default-members = ["specs", "core", "app", "test-suite"]
resolver = "3"

#[profile.release]
#lto = "fat"
#codegen-units = 1
#panic = "abort"

[profile.profiling]
inherits = "release"
debug = true
strip = false
